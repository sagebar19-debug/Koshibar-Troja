# KOSHIBAR 100% — Trojan Docker

Configuration Docker pour exécuter un serveur Trojan avec TLS.

## Structure

```text
/Koshibar/100%/
├── Dockerfile
├── docker-compose.yml
├── config.json
├── README.md
├── config/
│   └── config.json
└── tls/
    ├── fullchain.pem
    └── privkey.pem
